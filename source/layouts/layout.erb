<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv='X-UA-Compatible' content='IE=edge;chrome=1'>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= display_meta_tags site: 'work.stevegrossi.com' %>
    <%= stylesheet_link_tag :all %>
    <link href='http://fonts.googleapis.com/css?family=Lato:400,700,400italic,700italic' rel='stylesheet'>
    <link rel="alternate" type="application/atom+xml" title="work.stevegrossi.com Feed" href="/feed.xml" />
  </head>
  <body>
  <%= partial :analytics  %>
    <header role="banner">
      <a of="logo" href="/" title="Home">
        <h1>
          <strong>work</strong>.stevegrossi.com
        </h1>
      </a>
      <nav>
        <ul>
          <li><%= link_to('Blog', '/') %></li>
          <li><%= link_to('Portfolio', '/portfolio') %></li>
          <li><%= link_to('Principles', '/principles') %></li>
          <li><%= link_to('About', '//www.stevegrossi.com/is') %></li>
        </ul>
      </nav>
    </header>
    <div class="main" role="main">
      <%= yield %>
    <!--
    /
      aside
        h2 Recent Articles
        ol
          - blog.articles[0...10].each do |article|
            li= link_to(article.title, article)
        h2 Tags
        ol
          - blog.tags.each do |tag, articles|
            li
              = link_to(tag, tag_path(tag))
              | (#{ articles.size })
    -->
    </div>
    <footer class="site-footer">
      &copy; 2013 — <%= Time.now.year %> Steve Grossi | <%= link_to 'Feed', '/feed.xml' %>
    </footer>
    <%= javascript_include_tag :all %>
  </body>
</html>
